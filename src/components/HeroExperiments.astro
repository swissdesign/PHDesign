---
import type { HeroExperimentRow } from '../server/modules/cms';
import HeroExperimentsReact from './react/components/HeroExperimentsReact';

export interface Props {
  items: HeroExperimentRow[];
  lang?: string;
}

const { items = [], lang = 'de' } = Astro.props;
---

<HeroExperimentsReact items={items} lang={lang} client:load />
